{
  "name": "erplite_01",
  "version": "1.0.0",
  "description": "ERPLite is a lightweight ERPNext interface built with Frappe UI.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd Frontend && yarn install",
    "dev": "cd Frontend && yarn dev",
    "build": "cd Frontend && yarn build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "moment": "^2.30.1"
  }
}
